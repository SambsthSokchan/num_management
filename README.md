# NUM Management System

This is the Spring Boot implementation of the NUM Management System.

## 🚀 Setup & Run

1.  **Database**:
    - PostgreSQL required.
    - Create database: `num_management`.
    - Check `src/main/resources/application.properties` for credentials (user: `postgres`, pass: `Pa$$w0rd`).

2.  **Run**:
    ```bash
    mvn spring-boot:run
    ```

3.  **Access**:
    - http://localhost:8080/
    - Login: `/login` (Sign up first at `/signup`)

## 🎨 Design

- Matches "Seang Hai" Dashboard.
- Colors: Purple (`#DEDDFF`), Yellow (`#FEEFC3`), Pink (`#FCE7F3`).
- Technology: Spring Boot 3, Thymeleaf, PostgreSQL, Bootstrap/Custom CSS.
